# File created: 2011-08-06 16:04:23

include_rules

INCLUDES += $(INCLUDE)$(TUP_CWD)
INCLUDES += $(INCLUDE)$(TUP_CWD)/config

ROOT = $(TUP_CWD)
include $(ROOT)/mushspace.tup

: *.o space/*.o |> !ccshared |> libmushspace.so
: *.o space/*.o |> !ar |> libmushspace.a
