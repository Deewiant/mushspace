# File created: 2011-08-06 16:04:23

include_rules

ROOT = $(TUP_CWD)
include $(ROOT)/mushspace.tup

: *.o bounds/*.o space/*.o |> !ccshared |> libmushspace.so
: *.o bounds/*.o space/*.o |> !ar |> libmushspace.a
