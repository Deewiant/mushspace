# File created: 2011-08-06 16:04:23

include_rules

: foreach *.c         |> $(CC) $(CFLAGS) -c %f -o %o |> %B.o
: *.o lib/tommyds/*.o |> $(CC) $(LINKFLAGS) -shared %f -o %o |> libmushspace.so
: *.o lib/tommyds/*.o |> ar rcs %o %f |> libmushspace.a

.gitignore
